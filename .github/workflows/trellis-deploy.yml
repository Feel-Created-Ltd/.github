name: deploy

on:
  workflow_call:
    inputs:
      BEDROCK_DEPLOY_BRANCH:
        type: string
        default: "${{ github.ref_name }}"
        required: false
      NODE_VERSION:
        type: number
        default: 14
        required: false
      THEME_NAME:
        type: string
        default: ""
        required: false
      TRELLIS_ENVIRONMENT:
        type: string
        required: true
      TRELLIS_REPOSITORY:
        type: string
        required: true
    secrets:
      REPO_PAT:
        required: true
      TRELLIS_DEPLOY_SSH_PRIVATE_KEY:
        required: true
      TRELLIS_DEPLOY_SSH_KNOWN_HOSTS:
        required: true
      ANSIBLE_VAULT_PASSWORD:
        required: true
      ZEROTIER_CENTRAL_TOKEN:
        required: true
      ZEROTIER_GATEWAY_IP:
        required: true
      ZEROTIER_NETWORK_ID:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Trellis
        uses: actions/checkout@v4
        with:
          repository: "${{ inputs.TRELLIS_REPOSITORY }}"
          path: trellis
          token: "${{ secrets.REPO_PAT }}"
          ref: "${{ inputs.TRELLIS_ENVIRONMENT }}"

      - name: Get environment ZeroTier IP
        run: |
          HOST_IP="$(grep -Po 'ansible_host=\K.*' "hosts/${{ inputs.TRELLIS_ENVIRONMENT }}" )"
          echo "HOST_IP=${HOST_IP}" >> "${GITHUB_ENV}"
        working-directory: trellis

      - name: Join ZeroTier Network
        uses: zerotier/github-action@v1.0.3
        with:
          network_id: ${{ secrets.ZEROTIER_NETWORK_ID }}
          auth_token: ${{ secrets.ZEROTIER_CENTRAL_TOKEN }}

      - name: Route traffic through ZeroTier
        run: sudo ip route add "${HOST_IP}/32" via "${{ secrets.ZEROTIER_GATEWAY_IP }}"

      - name: Test connectivity to VPS
        run: |
          echo "Testing connection to ${HOST_IP}..."
          if ! ping -c 1 -W 2 ${HOST_IP}; then
            echo "❌ Cannot reach VPS over ZeroTier!"
            exit 1
          else
            echo "✅ VPS reachable over ZeroTier"
          fi

      - name: Setup SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          key: "${{ secrets.TRELLIS_DEPLOY_SSH_PRIVATE_KEY }}"
          known_hosts: "${{ secrets.TRELLIS_DEPLOY_SSH_KNOWN_HOSTS }}"

      - uses: webfactory/ssh-agent@v0.9.1
        with:
          ssh-private-key: "${{ secrets.TRELLIS_DEPLOY_SSH_PRIVATE_KEY }}"

      - uses: roots/setup-trellis-cli@v1.3.1
        with:
          ansible-vault-password: "${{ secrets.ANSIBLE_VAULT_PASSWORD }}"
          auto-init: true
          cache-virtualenv: true
          galaxy-install: true
          version: latest
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy
        working-directory: trellis
        run: trellis deploy --verbose --branch="${{ inputs.BEDROCK_DEPLOY_BRANCH }}" "${{ inputs.TRELLIS_ENVIRONMENT }}"
